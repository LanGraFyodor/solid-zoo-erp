# ERP-система Московского Зоопарка

Проект представляет собой реализацию программной подсистемы для управления зоопарком, разработанную с акцентом на применение архитектурных принципов SOLID.

## 📋 Предметная область
Программа моделирует основные бизнес-процессы современного зоопарка:
- **Учёт животных**: иерархическая структура различных видов (млекопитающие, птицы, рептилии).
- **Инфраструктура**: управление вольерами, контроль их вместимости и санитарного состояния.
- **Персонал**: разделение обязанностей между смотрителями (кормление, уборка) и ветеринарами (лечение).
- **Процессы**: автоматизация кормления по расписанию, проведение входных и плановых медосмотров.
- **Отчётность**: формирование сводок по состоянию популяции и инфраструктуры.

---

## 🏗️ Реализация принципов SOLID

При проектировании системы каждый из 5 принципов был учтен следующим образом:

### 1. Single Responsibility Principle (SRP)
**Принцип единственной ответственности**
Каждый класс выполняет строго одну задачу. Это позволяет изменять логику одного аспекта системы, не затрагивая остальные.

* **Примеры реализации:**
    * `FeedingService` занимается **только** управлением питанием и расписанием.
    * `VetService` отвечает **исключительно** за медицинские аспекты (осмотры, лечение, история болезни).
    * `ReportService` не хранит данные, а только **агрегирует и форматирует** их для вывода.
    * `Enclosure` управляет размещением животных, но не занимается их лечением или кормлением.

### 2. Open/Closed Principle (OCP)
**Принцип открытости/закрытости**
Система открыта для расширения, но закрыта для модификации.

* **Примеры реализации:**
    * Иерархия `Animal -> Mammal -> Lion`: Чтобы добавить новый вид животного (например, `Elephant`), нам не нужно менять код существующих классов или сервисов. Мы просто создаем новый класс, наследуемый от `Mammal`.
    * Сервисы (`FeedingService`, `VetService`) работают с абстракциями (`Animal`, `IFeedable`), поэтому они автоматически поддерживают любые новые виды животных без переписывания методов `feed()` или `examine()`.

### 3. Liskov Substitution Principle (LSP)
**Принцип подстановки Барбары Лисков**
Объекты подклассов должны заменять объекты базовых классов без нарушения корректности программы.

* **Примеры реализации:**
    * Все конкретные животные (`Lion`, `Parrot`, `Snake`) корректно реализуют методы базового класса `Animal`.
    * Где бы ни использовался тип `Animal` (в вольерах, в репозитории), мы можем передать туда любого наследника.
    * Пример в коде: `AbstractAnimal` задаёт базовое поведение, которое не "ломается" в наследниках. Змеи, птицы и звери — все "ведут себя" как животные (имеют имя, вид, состояние здоровья).

### 4. Interface Segregation Principle (ISP)
**Принцип разделения интерфейсов**
Клиенты не должны зависеть от интерфейсов, которые они не используют. Вместо одного "толстого" интерфейса создано несколько специализированных.

* **Примеры реализации:**
    * Вместо того чтобы поместить методы `feed()`, `heal()` и `clean()` в один интерфейс, они разделены на:
        * `IFeedable` — для процессов кормления.
        * `IHealable` — для ветеринарных процедур.
        * `ICleanable` — для обслуживания вольеров.
    * Это позволяет, например, создать сущность "Робот-уборщик", которая реализует `ICleanable`, но ей не нужны методы кормления или лечения.

### 5. Dependency Inversion Principle (DIP)
**Принцип инверсии зависимостей**
Модули верхнего уровня не зависят от модулей нижнего уровня. Оба зависят от абстракций.

* **Примеры реализации:**
    * Класс `Zoo` и сервисы (`FeedingService` и т.д.) не зависят от конкретного класса хранения данных `InMemoryAnimalRepository`.
    * Весь код завязан на интерфейс `IAnimalRepository`.
    * Это позволяет в будущем легко заменить хранение в памяти на базу данных (PostgreSQL, Mongo), просто написав новую реализацию интерфейса и передав её в конструктор `Zoo` (Dependency Injection), не меняя ни строчки бизнес-логики.

---

## 📈 Преимущества дизайна

### Почему абстракции улучшили качество?
1. **Тестируемость**: Благодаря DIP и DI, мы можем легко написать Unit-тесты для `FeedingService`, подставив "фейковый" репозиторий (Mock), и нам не нужен реальный доступ к БД или сложная настройка.
2. **Читаемость**: Код разбит на небольшие, логичные блоки. Разработчику не нужно читать 1000 строк класса `Zoo`, чтобы найти, как лечится лев. Он идёт в `VetService`.
3. **Безопасность изменений**: Изменяя формат отчёта в `ReportService`, мы гарантированно не сломаем логику кормления животных, так как эти модули никак не связаны напрямую.

### ⚠️ Потенциальные сложности расширения
Несмотря на гибкость, существуют сценарии, где могут возникнуть сложности:
1. **Изменение контрактов интерфейсов**: Если мы поймем, что метод `feed()` должен принимать не только `FoodType`, но и `amount` (количество), нам придется изменить интерфейс `IFeedable` и поправить **всех** его реализаторов (животных).
2. **Кросс-доменные правила**: Если появится правило "Кормить животное можно только если вольер убран", возникнет сложная зависимость между `FeedingService` и состоянием `Enclosure`, которую текущая архитектура не предусматривает явно (потребуется сервис-оркестратор).

---

## 🚀 Запуск проекта

> [!NOTE]
> Программа работает в **консольном режиме**. Основной сценарий демонстрации реализован в классе `Main.java`, который последовательно выполняет набор действий (наем сотрудников, прием животных, кормление, лечение, отчетность) для демонстрации возможностей системы.

![Скриншот работы программы](https://github.com/LanGraFyodor/solid-zoo-erp/blob/master/image.png)

Проект является стандартным Maven-приложением.

### Сборка и запуск демонстрации
```bash
mvn clean compile exec:java
```

При запуске будет выполнен сценарий `Main.java`, демонстрирующий:
1. Создание зоопарка и инфраструктуры.
2. Наем персонала.
3. Прием животных с прохождением медосмотра.
4. Работу расписания кормления.
5. Ветеринарные процедуры.
6. Генерацию итоговых отчётов.
